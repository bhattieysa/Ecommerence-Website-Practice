import type { ColorTheme, Size, Animation } from './SlidingAuth.variants';

export interface SlidingAuthProps {
  className?: string;
  colorTheme?: ColorTheme;
  size?: Size;
  animation?: Animation;
}
